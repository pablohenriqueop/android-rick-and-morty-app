<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="24dp"
        android:orientation="vertical"
        android:gravity="center"
        >

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:src="@drawable/logo"
            android:layout_gravity="center_horizontal"
            />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/home_first_text"
            android:textColor="?attr/colorPrimary"
            android:textSize="@dimen/font_size_md"
            android:layout_marginTop="@dimen/top_space_md"
            android:lineSpacingExtra="3sp"
            />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/home_second_text"
            android:textSize="@dimen/font_size_md"
            android:textColor="?attr/colorPrimary"
            android:layout_marginTop="@dimen/top_space_md"
            android:lineSpacingExtra="3sp"
            />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/charactersButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/home_first_button"
            android:backgroundTint="@color/grey"
            android:paddingVertical="@dimen/padding_y_md"
            app:cornerRadius="@dimen/full_radius"
            android:textColor="@color/dark"
            android:layout_marginTop="@dimen/top_space_md"
            />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/placesButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/home_second_button"
            android:backgroundTint="@color/grey"
            android:paddingVertical="@dimen/padding_y_md"
            app:cornerRadius="@dimen/full_radius"
            android:textColor="@color/dark"
            android:layout_marginTop="@dimen/top_space_md"
            />


    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>